---
phase: 01-feed-implementation
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified: [src/data.js, src/main.js, index.html, src/style.css]
autonomous: true
requirements: [FEED-01, FEED-02]

must_haves:
  truths:
    - "User can scroll through a list of dummy food posts seamlessly"
    - "Posts show deceptive captions and look like native social content"
  artifacts:
    - path: "src/data.js"
      provides: "Seed data representation"
      contains: "export const feedData"
    - path: "src/style.css"
      provides: "Native scroll mimicking CSS logic"
      contains: "overflow-y"
  key_links:
    - from: "src/main.js"
      to: "src/data.js"
      via: "Data import and rendering map"
      pattern: "import .* from .*data.js"
---

<objective>
Implement the scrollable, high-fidelity UI feed incorporating pre-seeded dummy data.

Purpose: Mocks a social media experience (Instagram style) satisfying FEED-01 and FEED-02. This establishes the context baseline before revealing deception in Phase 2.
Output: A static feed view packed with 5 dummy cards.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-feed-implementation/01-UI-SPEC.md
@.planning/phases/01-feed-implementation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate Dummy Data Store</name>
  <files>src/data.js</files>
  <read_first>@.planning/phases/01-feed-implementation/01-UI-SPEC.md</read_first>
  <action>Create `src/data.js`. Export a constant `feedData` array containing exactly 5 post objects. Structure: `{ id, authorName, authorHandle, avatarUrl, imageQuery, caption, likes }`. Use placeholder images for now via placeholder.com or Unsplash Source (e.g. `https://images.unsplash.com/photo-1546069901-ba9599a7e63c?w=600&h=600&fit=crop` for food, or simply solid colors until `generate_image` replaces them). Write highly persuasive, "deceptive influencer" style copy for each caption (e.g., claiming a loaded shake is "super healthy fuel").</action>
  <verify>grep "feedData =" src/data.js</verify>
  <acceptance_criteria>
    - src/data.js exports an array named feedData
    - Array contains 5 items
  </acceptance_criteria>
  <done>Mock baseline data is defined</done>
</task>

<task type="auto">
  <name>Task 2: Render Post Cards dynamically</name>
  <files>src/main.js, index.html</files>
  <read_first>src/data.js, index.html</read_first>
  <action>In `src/main.js`, create a rendering function `renderFeed(data)` that maps over `feedData` and injects HTML into `#feed-view`. The HTML template must include the header (avatar + name), main high-fidelity image (`<img>` with `loading="lazy"`), an interaction bar consisting of Heart/Message/Share inline SVG paths, and the deceptive caption below it. Once the app transitions from splash (Plan 1), `renderFeed(feedData)` should be called.</action>
  <verify>grep "renderFeed" src/main.js</verify>
  <acceptance_criteria>
    - src/main.js handles DOM insertion into feed-view
    - Post markup contains lazy loaded images
  </acceptance_criteria>
  <done>App populates the DOM dynamically based on data store</done>
</task>

<task type="auto">
  <name>Task 3: Apply Mobile-First scrolling & mock interaction</name>
  <files>src/style.css, src/main.js</files>
  <read_first>src/style.css</read_first>
  <action>In `src/style.css`, constrain `#feed-view` to match `01-UI-SPEC.md` formatting. Ensure posts have a full-width image behavior within the max 430px bounds. Use `overflow-y: auto`, `-webkit-overflow-scrolling: touch` to ensure scroll mimics native apps. Add a `.liked` CSS class (e.g., `fill: var(--color-destructive)`). In `src/main.js`, delegate a click listener for the interaction bar so clicking a Heart toggles the `.liked` class, fulfilling the mock immersion requirement.</action>
  <verify>grep "overflow-y" src/style.css</verify>
  <acceptance_criteria>
    - style.css contains native scroll flags
    - main.js has click delegation for interactions
  </acceptance_criteria>
  <done>Feed operates visually identically to a mocked Instagram application</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Ensure 5 dummy posts exist in `data.js`.
- [ ] Ensure images are loading with `lazy`.
- [ ] Verify JS events attach and toggle successfully on hearts.
</verification>

<success_criteria>
- All tasks completed
- Scrollable feed works smoothly
</success_criteria>

<output>
After completion, create `.planning/phases/01-feed-implementation/01-02-SUMMARY.md`
</output>
