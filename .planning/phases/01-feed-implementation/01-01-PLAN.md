---
phase: 01-feed-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, index.html, src/main.js, src/style.css]
autonomous: true
requirements: [BEHAV-01]

must_haves:
  truths:
    - "User can view a splash screen with an age demographic selector"
    - "System captures age cleanly and transitions away from the splash screen"
  artifacts:
    - path: "index.html"
      provides: "Splash screen markup"
      contains: "id=\"age-selector\""
    - path: "src/main.js"
      provides: "Session state management"
      contains: "sessionStorage"
  key_links:
    - from: "index.html"
      to: "src/main.js"
      via: "Script inclusion"
      pattern: "<script type=\"module\" src=\"/src/main.js\">"
---

<objective>
Scaffold the core vanilla Vite project and implement the primary welcome/age-selector flow to capture the user's demographic frictionlessly.

Purpose: Establishes the application foundation and satisfies BEHAV-01 without involving complex backend auth.
Output: Initialized web project and the working splash UI.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-feed-implementation/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite Project & Reset</name>
  <files>package.json, vite.config.js, index.html, src/main.js, src/style.css</files>
  <read_first>@.planning/phases/01-feed-implementation/01-UI-SPEC.md</read_first>
  <action>Run `npx -y create-vite@latest . --template vanilla` from the project root. Clean the default counter boilerplate. Set up `index.html` referencing `/src/main.js` and `/src/style.css`. Define the base CSS variables in `style.css` based on `01-UI-SPEC.md` (e.g., `--color-primary`, spacing variables).</action>
  <verify>test -f package.json && grep "vite" package.json</verify>
  <acceptance_criteria>
    - package.json contains "vite"
    - src/style.css contains base UI-SPEC variables
  </acceptance_criteria>
  <done>Vite is configured and UI-SPEC base tokens are stored in CSS</done>
</task>

<task type="auto">
  <name>Task 2: Build Splash & Age Selector</name>
  <files>index.html, src/style.css</files>
  <read_first>index.html, src/style.css</read_first>
  <action>Add a `div id="splash-view"` to `index.html`. Add an `h1` "CraveCheck", an `h2` "Select your age group to begin the simulation", a `<select id="age-selector">` with options (Under 18, 18-24, 25-34, 35-44, 45+), and a `<button id="start-btn">` "Start Scrolling". Apply Vanilla CSS utilizing the mobile-first max-width (430px) container constraint defined in UI-SPEC.</action>
  <verify>grep 'id="splash-view"' index.html</verify>
  <acceptance_criteria>
    - index.html contains `<div id="splash-view">`
    - index.html contains `<select id="age-selector">`
  </acceptance_criteria>
  <done>Splash elements are present in DOM</done>
</task>

<task type="auto">
  <name>Task 3: Implement Age Session Storage Logistics</name>
  <files>src/main.js</files>
  <read_first>index.html</read_first>
  <action>Write Javascript in `src/main.js` to attach a click listener to `#start-btn`. On click, read the value of `#age-selector`, write it to `sessionStorage.setItem('cravecheck_age', val)`, hide the `#splash-view` (e.g. `display: none`), and show a `#feed-view` (create a dummy empty `<div id="feed-view" style="display:none">` in index.html for now).</action>
  <verify>grep "sessionStorage.setItem" src/main.js</verify>
  <acceptance_criteria>
    - src/main.js contains sessionStorage.setItem
    - src/main.js contains click listener logic for start button
  </acceptance_criteria>
  <done>Session logic handles transitions</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `package.json` exists with vite.
- [ ] UI base styling variables are initialized.
- [ ] Age selector HTML is complete.
</verification>

<success_criteria>
- All tasks completed
- Validated via grep/cat tools
</success_criteria>

<output>
After completion, create `.planning/phases/01-feed-implementation/01-01-SUMMARY.md`
</output>
